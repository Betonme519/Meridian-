// Planner API: semester plan / requirement check / save & load
export async function getPlan(_userId: string) {
  return null;
}

export async function savePlan(_userId: string, _plan: unknown) {
  return null;
}
