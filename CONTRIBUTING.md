# 贡献指南

欢迎提交界面问题、可复现的计算偏差、规则适配建议与聚焦的功能改进。

## 报告问题

说明操作步骤、预期结果、实际结果及运行模式（mock 或 remote）。规则相关问题请注明学校、适用年级、文件版本与公开来源；用虚构数据复现，不上传私人资料或密钥。

## 提交改动

1. 阅读 `AGENTS.md`、`CLAUDE.md` 与相关架构说明。
2. 保持改动聚焦，复用已有组件与数据访问层。
3. 检查 `npm run build` 和 `npm run lint`，说明实际完成的验证。
4. 影响推荐或模拟时，检查空数据、未完成事项、已完成事项及模型失败回退。
5. 数据库改动同时考虑 RLS 与对应校验脚本。

仓库当前没有统一 `npm test` 命令。不要把构建通过等同于功能、规则或安全验证。提交说明应明确哪些场景实际检查过。

提交前移除个人配置、构建产物与无关格式变化。第三方规则资料和素材需要明确来源与使用权限。
