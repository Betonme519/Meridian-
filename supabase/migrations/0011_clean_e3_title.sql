-- ─────────────────────────────────────────────────────────────────────
-- 0011_clean_e3_title — 去掉 E3-1 标题里冗余的「/ 3 模块」
--
-- 背景：0005 seed E3-1 title 写成「通识教育总学分 (8 学分 / 3 模块)」，
--      UI 上「3 模块」对学生没意义（modules 信息已在 metadata jsonb），
--      用户 2026-05-27 反馈"不必要"。
--
-- 幂等：WHERE 只匹配旧值；已是新值的库不会被改回。
-- ─────────────────────────────────────────────────────────────────────

-- ============================================================
-- UP — apply this migration
-- ============================================================

UPDATE public.track_requirement
SET title = '通识教育总学分 (8 学分)',
    updated_at = now()
WHERE code = 'E3-1'
  AND title = '通识教育总学分 (8 学分 / 3 模块)';

-- ============================================================
-- DOWN — 注释保留，手工回滚参考
-- ============================================================
-- UPDATE public.track_requirement
-- SET title = '通识教育总学分 (8 学分 / 3 模块)',
--     updated_at = now()
-- WHERE code = 'E3-1'
--   AND title = '通识教育总学分 (8 学分)';
