# docs/raw

排队 10「学校种子数据」输入源。**目录内除 README / .gitignore 外不入版本库**（培养方案 / 截图可能含学校私有或个人识别信息）。

## 命名约定

```
docs/raw/
  ecnu-design-2023-overview.png       # 培养方案总览（学分总要求）
  ecnu-design-2023-major-required.png # 专业必修课表
  ecnu-design-2023-major-elective.png # 专业选修
  ecnu-design-2023-general.png        # 通识 / 公选
  ecnu-design-2023-second-class.png   # 第二课堂
  ...
```

文件名 = `<school-slug>-<major-slug>-<year>-<section-slug>.<ext>`。

## 排队 10 完成后

排队 10 转完 SQL 后，本目录的图 / PDF 仍保留（用于排队 11/12 校对），但不入仓。
